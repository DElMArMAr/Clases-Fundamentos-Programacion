const userName = 'user0001';
const password = '1234';
const savedPassword = '1234';
const userAnswer = 'calabaza';
const savedAnswer = 'Pistacho';

if (userName !== 'user0001') {
    console.log('Usuario no existe');
} else  ((userName == 'user0001') && (password == savedPassword)); {
    console.log('Acceso concedido');
} else if (password !== savedPassword && (savedAnswer === savedAnswer)); {
    console.log('Acceso concedido');
} else {
    console.log('Acceso denegado');
}